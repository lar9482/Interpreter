def int gcd(int x, int y) {
    int result;
    int holder;

    result = x;
    holder = y;
    
    while (holder != 0) {
        int temp;
        temp = holder;
        holder = result % holder;
        result = temp;
    }

    return result;
}

def int main() 
{ 
    int first_int;
    int second_int;

    first_int = 5444;
    second_int = 64;
    print_int(gcd(first_int, second_int));

    return 0;
}