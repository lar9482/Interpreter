def int factorial(int n) {
    if (n <= 0) {
        return 1;
    }
    else {
        return (n * factorial(n-1));
    }
}

def int main() 
{ 
    int n;
    n = 5;
    
    print_int(factorial(n));

    return 0;
}